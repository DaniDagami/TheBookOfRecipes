@model IEnumerable<TheBookOfRecipes.Models.Recipe>

<style>
</style>
<link href="~/css/navigation.css" rel="stylesheet" />
<!--
<header>
    <div id="hero-left-block">
        <h1 id="hero-name">Nikolay Tabalyov</h1>
        <h3 id="hero-title"><i>Developer | Problem Solver</i></h3>
    </div>
    <nav id="nav-box">
        <a href="#introduction" class="nav-link">Home</a>
        <a href="#skills" class="nav-link">Skills</a>
        <a href="#projects" class="nav-link">Projects</a>
        <a href="#testimonials" class="nav-link">Testimonials</a>
        <a href="#contacts" class="nav-link">Contact</a>
    </nav>
</header> --->

<h1>Recipes</h1>

<div class="row">
    @foreach (var recipe in Model) {
        <div class="col-md-4">
            <div class="card">
                <img src="@recipe.ImageUrl" class="card-img-top" alt="@recipe.Name" />
                <div class="card-body">
                    <h5 class="card-title">@recipe.Name</h5>
                    <p class="card-text">@recipe.Description</p>
                    <a asp-controller="Recipes" asp-action="Details" asp-route-id="@recipe.RecipeId" class="btn btn-primary">Details</a>
                </div>
            </div>
        </div>
    }
</div>

<script>
    document.addEventListener("scroll", function () {
        const header = document.querySelector("header");
        if (window.scrollY > 50) {
            header.classList.add("small");
        } else {
            header.classList.remove("small");
        }
    });
</script>